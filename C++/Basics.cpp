/*To take input to produce output we use the library iostream
For mathematical functional we use math.h*/

/*if we use namespace std library we do not have to use std again and again...it autmatically uses std for every function*/

/*std -  is the one under which cout comes
endl - is used to print something on next line*/

/*#include<bits/stdc++.h> : includes every single library required for program. and includes all data types too*/

#include<iostream>
using namespace std;
int main(){
    int x ,y;
    cin>>x >> y;   //way of taking single input or multiple input
    cout<<"Value of x"<<x << y;
    return 0;
}